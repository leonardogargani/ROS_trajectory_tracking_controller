#
# source: https://github.com/ros-planning/navigation/blob/melodic-devel/costmap_2d/launch/example_params.yaml
#

my_global_costmap:
    global_frame: map #odom #map   # topic where the map_server publishes in (maybe?)
    robot_base_frame: base_link
    #update_frequency: 5.0
    #publish_frequency: 1.0

    #resolution: 1.0

    #static_map: true
    #first_map_only: true
    #map_type: costmap

    plugins:
        - {name: static_layer, type: "costmap_2d::StaticLayer"}
    static_layer:
        map_topic: map

    #transform_tolerance: 0.3
    #obstacle_range: 2.5
    #max_obstacle_height: 2.0
    #raytrace_range: 3.0
    #footprint: [[-0.05, -0.1], [-0.05, 0.1], [0.05, 0.1], [0.1, 0.0], [0.05, -0.1]]
    robot_radius: 0.15
    #footprint_padding: 0.01
    #inflation_radius: 0.55
    #cost_scaling_factor: 10.0
    #lethal_cost_threshold: 100


#my_local_costmap:
#    global_frame: odom
#    robot_base_frame: base_link
#    update_frequency: 5.0
#    publish_frequency: 1.0
#    
#    plugins:
#        - {name: static_layer, type: "costmap_2d::StaticLayer"}
#    static_layer:
#        map_topic: "/map"
