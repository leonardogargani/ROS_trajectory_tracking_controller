required packages to be installed
---------------------------------
- sudo apt install ros-melodic-navigation ros-melodic-dwa-local-planner
- sudo ln -s /usr/include/eigen3/Eigen /usr/include/Eigen
    \__ (dwa needs looks for /usr/include/Eigen, which I have in /usr/include/eigen3/Eigen)



People who used DWA standalone
------------------------------
- https://github.com/amslabtech/dwa_planner
- https://answers.ros.org/question/376753/dwa-local-planner-as-a-stand-alone-c-library



From https://wiki.ros.org/dwa_local_planner:
- Published Topics:
    - ~<name>/global_plan (nav_msgs/Path)
    - ~<name>/local_plan (nav_msgs/Path)
- Subscribed Topics:
    - odom (nav_msgs/Odometry)

From the nav_core image [looking at the local_planner circle]:
- Published Topics:
    - cmd_vel (geometry_msgs/Twist)
- reads the nav_msgs/Path internally sent by the global_planner
- Subscribed Topics:
    - odom (nav_msgs/Odometry)


Navigation stack
----------------
- https://wiki.ros.org/navigation/Tutorials




--------------------------------------------------------------------


Things to try:
- modify parameters in dwa_planner_params.yaml
- change local/global costmap and pose
- change the tf in launch file
- check position of the map wrt to the goal



In isGoalReched(), when doing costmap_ros_->getRobotPose(current_pose_) the value of current_pose_ is overwritten to 0.0 (while it was previously correct thanks to our function dp.setCurrentPose(gl_l_global_pose))



Increasing sim_time to 15.0 makes the simulation a circumference
-> it seems that the goal is in the origin



--------------------------------------------------------------------


- getRobotPose() (funzione di Costmap2D) ritorna sempre 0 (= parametro della static tf)
    - magari qualche frame in costmap_params.yaml deve cambiare?
    - magari qualche tf nel launch file deve cambiare da statica a dinamica?
        - nel diagramma di move_base si vede che odom viene usata per pubblicare una tf    

- la footprint in rviz rimane fissa nell'origine

- usando ostacolo a dritto non si ferma




















